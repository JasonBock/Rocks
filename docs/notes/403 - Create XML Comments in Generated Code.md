* Create
    * DONE - Expectations
* Make

TODO:
* DONE - For a bit of brevity, the generated code in a handler that does this: `if (this.parent.handlers0 is null) { this.parent.handlers0 = new(1); }` can be shorted to this: `this.parent.handlers0 ??= new(1);`